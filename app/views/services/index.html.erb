<%= link_to root_path, class: "link-back" do %>
    Back
<% end %>

<div class="two-thirds">
    <h1 class="page-title">These groups and services may be able to help</h1>
</div>

<ol class="service-results">
    <% @top_result.each do |s| %>
        <%= render partial: "shared/service_card", locals: {s: s, recommended: true} %>
    <% end %>

    <% @services.each do |s| %>
        <%= render partial: "shared/service_card", locals: {s: s, recommended: false} %>
    <% end %>
</ol>

<section class="safeguarding">
    <h2>Can't find what you need?</h2>
    <p>If you’re worried about yourself or someone you care about, <a href="https://www.essex.gov.uk/topic/coronavirus">see our coronavirus hub</a> for the latest information and advice.</p>
</section>
